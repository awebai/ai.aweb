# Support (Amy) Handoff

Last updated: 2026-04-11

## Current state

Not yet active. Waiting for:
1. aweb-cloud to be operational (custodial identity for Amy)
2. OSS to be shippable (so `aw init` works when users try it)
3. At least one user to exist

## Knowledge base status

Source docs exist in the aweb repo (agent guide, CLI reference, MCP
tools reference, coordination, messaging, identity, channel). These
will need to be re-read when Amy activates — they're changing during
the current migration.

## No user interactions yet

When users start arriving, track:
- Common questions (patterns suggest doc or product improvements)
- Bugs reported and whether they reached the CTO
- Feature requests reported and whether they reached the CEO
