# aweb 1.18.4 / KI#1 — Charlene handoff (WAS 1.18.3)

> **STILL HOLD as of 2026-04-26 evening — substance recovery in flight.**
>
> **Update**: aweb 1.18.4 published 2026-04-26T16:58Z (PyPI) + 17:00Z (npm).
> Fixes Mode 1 (hosted-custodial mail HTTP 422 binding regression
> introduced in 1.18.3). Substance: server-side `_recipient_identity_matches`
> helper accepts identity-equivalent bindings (same agent_id OR did_aw OR
> did_key). Tom + Amy mailed for empirical attestation on Mode 1.
>
> **Mode 2 remains open**: cross-team-cert chat path produces empty
> to_did wires (Amy's eb0f6cc5 case). Confirmed REAL on 1.18.3 (stale-
> binary ruled out cleanly). Cross-team-cert agents should use mail
> (post-1.18.4) or pin-based workaround for chat until 1.18.5 ships
> the chat-path fix.
>
> **Until BOTH Tom + Amy attest empirically green on Mode 1**: framing
> stays "ships substance for Mode 1; Mode 2 separate investigation."
> Migrating to "closes for Mode 1" only after both empirical green.
>
> **Mode 2 closure waits for 1.18.5**.
>
> Banked discipline lessons (cycle case-study, Randy + I drafted unified
> 5-policy proposal to Juan):
> - `feedback_reproducer_synthetic_state_assumes_user_invariants.md`
> - `feedback_no_closes_framing_pre_empirical_attestation.md`
> - `feedback_bug_class_spans_architectural_layers.md`
> - `feedback_no_trivial_fix_exception_release_bound.md`
>
> Standing release-discipline post-Juan-sign-off (proposal pending):
> empirical-attestation gate / coverage-gap audit pre-release / risk-flag-
> to-test conversion / hosted-custodial e2e matrix (v0.5.9) / staged
> distribution.
>
> Do not draft external content until BOTH attestations lands GREEN
> on Mode 1 AND Mode 2 fix lands in 1.18.5.

**Date**: 2026-04-26
**From**: John (coord-aweb)
**Status**: PyPI live (`aweb 1.18.3`); npm (`@awebai/aw 1.18.3`) propagating; **empirical attestation FAILED, 1.18.4 fix in flight**

## Locked ship-mail framing for KI#1

Use this language — Randy framing-locked, Tom verified, gates green:

> KI#1 closes for continuity case (aalk: known-agent pin fallback when 
> registry resolution misses or returns empty) and for authenticated 
> first-contact (aalm: CLI signs address lookups for org_only / 
> team_members_only visibility). Wholesale closure attestation pending 
> empirical re-test post-publish + CLI upgrade.

## What shipped in 1.18.3

Two cross-team-cert messaging fixes:

- **aalk** (continuity-fallback): when the CLI tries to resolve a recipient's address but the registry returns 404, the CLI now falls back to the local known-agents pin store. Closes the case where the sender already has the recipient pinned from prior contact (e.g., reply scenarios).

- **aalm** (authenticated lookup): the CLI now signs registry GET requests using the existing awid signed-read scheme (Authorization: DIDKey + X-AWEB-Timestamp + canonical payload). Closes the case where the recipient's awid row exists with `org_only` or `team_members_only` reachability — anonymous CLI lookups previously 404'd those rows even though the sender was authorized.

Plus 2 server-side fixes for asymmetric branches in messages.py that surfaced when the CLI started populating `to_did` on address-routed mail (previously latent bugs).

## What does NOT ship in 1.18.3

Per discipline, the release announcement should be honest about residual scope:

- Full architectural audit of the elif-branch parity in messages.py is queued for v0.5.9 (one minor third asymmetry still latent — registry-client-None handling).
- A few low-priority CLI polish items (aaln) and awid server-side correctness items (aalo, aalp) are tracked but unrelated to KI#1.

## Note on cloud (ac)

aweb 1.18.3 is the new pin target for **ac v0.5.9** (Tom's call on timing — gates on leg-2 dashboard probe for aalf empirical attestation). When v0.5.9 ships, app.aweb.ai gets the same aweb substance as CLI users.

## What you might want to say externally

If you're drafting an external announcement (blog, social, etc.), the angles I'd suggest:

1. **For the existing community**: "Cross-team messaging now resolves authorized teammates without a prior pin." Concrete improvement, scoped technical.

2. **For prospects on the fence**: pair with the org-only-reachability-tier story — agents in the same org can now find each other without operator-side intervention.

3. **Honest framing**: hold "wholesale closure" language until users empirically attest. Use "improves" and "closes for [specific case]" instead.

Don't draft against a "fixes everything" story — it overstates scope and the user-empirical attestation hasn't happened yet (Amy is the first probe; she'll re-test once npm propagates).

## Coordination

If you have questions on framing or want me to verify any technical claim before publishing, mail or chat me. I'm coord-aweb (alias `john`).
